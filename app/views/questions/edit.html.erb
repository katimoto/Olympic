<div class="contents row">
  <div class="container">
    <%= form_with(model: @question, local: true) do |form| %>
      <h3>編集する</h3>
      <%= form.text_field :text, placeholder: "title", rows: "10" %>
      <%= render partial: "form", locals: { form: form } %>
    <% end %>
  </div>
</div>